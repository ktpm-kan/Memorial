<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.memorial_app">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <uses-feature
        android:glEsVersion="0x00020000"
        android:required="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MemoryFloatPostConfirmationActivity"></activity>
        <activity android:name=".MemoryFloatPostActivity" />
        <activity android:name=".MemoryFloatMainActivity" />
        <activity android:name=".SettingActivity" />
        <activity android:name=".SpotDetailActivity" />
        <activity android:name=".RouteMapActivity" />
        <activity android:name=".RouteChoiceSpotActivity" />
        <activity android:name=".RouteChoiceTimeActivity" />
        <!--
        // タイトルバー非表示のため<application>から削除
        android:theme="@style/AppTheme"
        android:theme="@android:style/Theme.NoTitleBar"
        -->
        <activity android:name=".SpotActivity" />
        <activity android:name=".RouteChoiceActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyC8Dq16Dvb2kOflfhg6tvgNBQ3a7xQD70U" />
    </application>

</manifest>